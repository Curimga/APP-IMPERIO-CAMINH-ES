# Supabase do CRM — PROJETO REAL (NÃO é o projeto independente fjvctjizmvvkbbstrobx)
# Copie as chaves do .env do app (ou do .env do CRM) para as variáveis abaixo.
# Nunca comite a chave real; este arquivo é apenas um modelo de nomes.
VITE_SUPABASE_URL=https://<SEU-PROJECT-REF>.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=cole_a_publishable_key_anon_aqui